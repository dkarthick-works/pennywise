services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ledger-app
    env_file: .env
    ports:
      - "8084:8080"
    restart: unless-stopped
    networks:
      - coolify

networks:
  coolify:
    external: true
